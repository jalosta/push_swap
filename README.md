This project has been created as part
of the 42 curriculum by jalosta-

Description:-
- Program: push_swap
- Goal: sort stack a in ascending order with the least effort possible using operations
- Input: stack a, containing a random number of unique negative and\or positive integers
- Output: the minimal set of operations to sort the stack. instructions printed in terminal delimited by newlines, eligible input to checker
Instructions:

Resources:
